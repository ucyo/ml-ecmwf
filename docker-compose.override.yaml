version: "3.8"

services:

  redis:
    volumes:
      - ./code/rstore:/data

  code:
    volumes:
      - ./code:/home/python/code
    ports:
      - "9181:9181"

  worker:
    volumes:
      - ./code:/home/python/code
