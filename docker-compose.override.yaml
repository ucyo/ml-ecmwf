version: "3.8"

services:

  rq-dashboard:
    ports:
      - "9181:9181"

  redis:
    volumes:
      - ./code/rstore:/data

  code:
    volumes:
      - ./code:/home/python/code
    ports:
      - "8080:8080"

  worker:
    volumes:
      - ./code:/home/python/code
