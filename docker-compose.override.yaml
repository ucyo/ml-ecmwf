version: "3.8"

services:

  rq-dashboard:
    ports:
      - "9181:9181"

  redis:
    volumes:
      - ./code/rstore:/data

  code:
    user: "$uid:$gid"
    volumes:
      - ./code:/home/python/code
      - /abcde:/abcde

  worker:
    user: "$uid:$gid"
    volumes:
      - ./code:/home/python/code
      - /abcde:/abcde
